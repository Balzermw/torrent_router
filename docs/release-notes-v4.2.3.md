# Synology Torrent Router v4.2.3

Small UI bugfix release.

## Fixed

- Prevents Torrent Router select labels from overlapping the placeholder or selected text in the initial send dialog.
- Keeps the Favorite destination, Preset, and Recent for tracker labels pinned above their select values.

## Notes

This build remains a fork of [`dvcol/synology-download`](https://github.com/dvcol/synology-download), MIT licensed, with attribution preserved.
